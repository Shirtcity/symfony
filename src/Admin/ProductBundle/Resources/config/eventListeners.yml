services:

    #
    # Event listeners
    #
    elcodi_admin.event_listener.category_position:
        class: Admin\ProductBundle\EventListener\NewCategoryPositionEventListener
        tags:
            - { name: doctrine.event_listener, event: preFlush, method: preFlush }
