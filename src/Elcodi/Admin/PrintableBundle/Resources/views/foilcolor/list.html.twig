{% extends 'AdminCoreBundle::layout.html.twig' %}


{% set elcodi_context = {
    entity_type: 'foilcolor'
} %}


{% block breadcrumb %}

    {% include '@AdminCore/Common/breadcrumb.html.twig' with {
        breadcrumb: [
            { name: 'admin.foilcolor.plural'|trans, active: true },
        ],
    } %}

{% endblock breadcrumb %}


{% block header_actions %}

    <a href="{{ url('admin_foilcolor_new') }}" type="button" class="button-primary" data-test="new-foilcolor">
        <i class="icon-plus"></i>
        <span>{{ 'admin.foilcolor.new'|trans }}</span>
    </a>

{% endblock header_actions %}


{% block content %}

    {{
        render(url('admin_foilcolor_list_component', {
            page: page,
            limit: limit,
            orderByField: orderByField,
            orderByDirection: orderByDirection
        }))
    }}

{% endblock content %}
