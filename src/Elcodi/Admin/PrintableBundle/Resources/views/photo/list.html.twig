{% extends 'AdminCoreBundle::layout.html.twig' %}


{% set elcodi_context = {
    entity_type: 'photo'
} %}


{% block breadcrumb %}

    {% include '@AdminCore/Common/breadcrumb.html.twig' with {
        breadcrumb: [
            { name: 'admin.photo.plural'|trans, active: true },
        ],
    } %}

{% endblock breadcrumb %}


{% block header_actions %}

    <a href="{{ url('admin_photo_new') }}" type="button" class="button-primary" data-test="new-photo">
        <i class="icon-plus"></i>
        <span>{{ 'admin.photo.new'|trans }}</span>
    </a>

{% endblock header_actions %}


{% block content %}

    {{
        render(url('admin_photo_list_component', {
            page: page,
            limit: limit,
            orderByField: orderByField,
            orderByDirection: orderByDirection
        }))
    }}

{% endblock content %}
