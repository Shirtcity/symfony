{% extends 'AdminCoreBundle::layout.html.twig' %}


{% set elcodi_context = {
    entity_type: 'article'
} %}


{% block breadcrumb %}

    {% include '@AdminCore/Common/breadcrumb.html.twig' with {
        breadcrumb: [
            { name: 'admin.article.plural'|trans, active: true },
        ],
    } %}

{% endblock breadcrumb %}


{% block header_actions %}

    <a href="{{ url('admin_article_new') }}" type="button" class="button-primary" data-test="new-article">
        <i class="icon-plus"></i>
        <span>{{ 'admin.article.new'|trans }}</span>
    </a>

{% endblock header_actions %}


{% block content %}

    {{
        render(url('admin_article_list_component', {
            page: page,
            limit: limit,
            orderByField: orderByField,
            orderByDirection: orderByDirection
        }))
    }}

{% endblock content %}
