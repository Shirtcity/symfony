{% extends 'AdminCoreBundle::layout.html.twig' %}


{% set elcodi_context = {
    entity_type: 'variant'
} %}


{% block body %}

    {% if variant.id %}
        {{
            render(url('admin_article_variant_edit_component', {
                articleId: article.id,
                id: variant.id,
            }))
        }}
    {% else %}
        {{
            render(url('admin_article_variant_new_component', {
                articleId: article.id,
            }))
        }}
    {% endif %}

{% endblock body %}
